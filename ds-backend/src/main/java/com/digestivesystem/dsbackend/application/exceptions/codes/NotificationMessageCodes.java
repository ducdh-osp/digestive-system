package com.digestivesystem.dsbackend.application.exceptions.codes;

/** Mã lỗi dùng chung bởi NotificationService (Customer) và AdminNotificationService (Admin). */
public final class NotificationMessageCodes {

    public static final String NOT_FOUND = "error.notification.not-found";

    private NotificationMessageCodes() {
    }
}
